# In production, use a secrets manager (Vault, Sealed Secrets, etc.)
# Generate base64 values with: echo -n "your-value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: agentos-secrets
  namespace: agentos
type: Opaque
data:
  # echo -n "sk-replace-with-your-key" | base64
  OPENAI_API_KEY: c2stcmVwbGFjZS13aXRoLXlvdXIta2V5
  # echo -n "ai" | base64
  DB_PASS: YWk=
